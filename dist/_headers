/*
  # Allow embedding the site inside Wix (portfolio) and your domain.
  # If you later need stricter hostnames, replace with your exact Wix domain(s).
  Content-Security-Policy: frame-ancestors 'self' https://*.wix.com https://*.wixsite.com https://henigb.com https://www.henigb.com

  # Sensible security headers
  Referrer-Policy: strict-origin-when-cross-origin
  X-Content-Type-Options: nosniff
  X-XSS-Protection: 0
